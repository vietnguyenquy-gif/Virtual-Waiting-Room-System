# Virtual-Waiting-Room-System
A high-concurrency virtual waiting room and rate-limiting engine built to handle spike traffic during peak events. Uses Redis in-memory queueing, token bucket rate limiting, and async message processing to protect core databases from server crashes under heavy loads.
